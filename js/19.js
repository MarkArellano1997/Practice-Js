const saldo = 800
const pagar = 700
const tarjeta = true

// || operador logico o
// && operador logico y

if (saldo >pagar && tarjeta) {
    console.log("Puede pagar");
}
else{
    console.log("No, no puedes pagar");
}